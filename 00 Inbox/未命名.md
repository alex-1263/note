```mermaid
graph TD;
  A[开始] -->|数据初始化| B(定义PCB结构体)
  B --> C(创建进程队列)
  C -->|FCFS算法| D(按到达时间排序)
  D --> E(遍历进程队列)
  E --> F(记录开始和结束时间)
  F --> G(计算性能指标)
  G --> H[结束]

  C -->|优先级调度算法| I(按优先级排序)
  I --> J(遍历进程队列)
  J --> K(记录开始和结束时间)
  K --> L(计算性能指标)
  L --> H
  H -->|结束| Z[结束]
